<form method="post" action="{{ path('app_rendus_activites_delete', {'id': rendus_activite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendus_activite.id) }}">
    <button class="btn">Delete</button>
</form>
