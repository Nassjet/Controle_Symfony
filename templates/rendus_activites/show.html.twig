{% extends 'base.html.twig' %}

{% block title %}RendusActivites{% endblock %}

{% block body %}
    <h1>RendusActivites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendus_activite.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ rendus_activite.url }}</td>
            </tr>
            <tr>
                <th>DateHeure</th>
                <td>{{ rendus_activite.dateHeure ? rendus_activite.dateHeure|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendus_activites_index') }}">back to list</a>

    <a href="{{ path('app_rendus_activites_edit', {'id': rendus_activite.id}) }}">edit</a>

    {{ include('rendus_activites/_delete_form.html.twig') }}
{% endblock %}
